#!/bin/bash

# Install macOS dependencies for obs-audio-to-websocket
# This script is called automatically by the build-macos script

set -e

echo "Installing macOS dependencies..."

# Dependencies are managed via .Brewfile
# The build-macos script handles Homebrew installation and bundle install

echo "Dependencies installed via Brewfile"