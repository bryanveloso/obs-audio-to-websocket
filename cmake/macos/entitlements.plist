<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for hardened runtime -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    
    <!-- Required for loading into OBS -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    
    <!-- Optional: Required if plugin needs JIT -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    
    <!-- Optional: Required if plugin uses dyld environment variables -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    
    <!-- Network access for WebSocket connections -->
    <key>com.apple.security.network.client</key>
    <true/>
    
    <!-- Audio input access (if needed for future features) -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>